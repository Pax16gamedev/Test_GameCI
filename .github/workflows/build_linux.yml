name: Build Linux

on:
  workflow_dispatch:

jobs:
  build-linux:
    uses: Pax16gamedev/TestGameCI_DevOps/.github/workflows/build-linux.yml@DESARROLLO
    with:
      unityVersion: 6000.2.10f1
      sendToDiscord: true
    secrets: inherit